function Calculator() {

    this.add = function (x, y) {
        return x + y;
    };

    this.substract = function (x, y) {
        return x - y;
    };

    this.multiply = function (x, y) {
        return x * y;
    };

    this.divide = function (x, y) {
        return x / y;
    };
};